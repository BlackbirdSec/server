# BlackBird Server v1.0-Beta

#### To Developments

Use **cmake** to generate Makefiles for the **blackbird server**.

```sh
cmake CMakeLists.txt
```

Now, you can run `make all` or `make blackbird` to build project.
